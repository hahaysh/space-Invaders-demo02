# 우주 방어 기술 설계

제품 수치와 동작의 기준은 `PRD.md`다. 이 문서는 구현·검증 책임만 정의한다.

## 기술과 책임

HTML·CSS·ES Modules와 Canvas 2D로 구현한다. 작은 순수 게임 모델과 브라우저 UI로 나눈다.
모델은 초기 상태, 초 단위 시간 갱신, 이동·발사·편대·충돌·점수·승패를 담당한다.
UI는 DOM 버튼·키 입력, Canvas 그리기, 점수·결과 표시를 담당하며 모델은 DOM을 참조하지 않는다.
구체적인 모듈 개수와 함수명은 구현 시 필요한 만큼만 정한다.

단일 requestAnimationFrame 루프에서 초 단위 delta를 계산한다.
긴 프레임은 delta 제한과 작은 하위 갱신으로 과도한 이동·충돌 통과를 방지한다.
playing에서만 규칙 시간을 전진시키고 충돌 처리 후 승리, 그 다음 방어선 패배를 판단한다.
탄환과 적의 제거는 한 번만 반영한다.

이벤트는 한 번만 연결한다. 키 집합으로 반대 방향 동시 입력을 상쇄하며 반복 단발키를 무시한다.
blur/visibilitychange에서 입력을 비우고 첫 버전에서는 상태를 자동 일시정지하지 않는다.
상태 진입·재시작 시 입력 및 시간 기준을 정리하고 새 모델로 PRD 초기화 조건을 충족한다.
화면 전환 중 중복 루프나 리스너가 생기지 않도록 반복 시작·재시작을 검증한다.

## 검증

| 경로 | 책임 |
|---|---|
| Node 내장 `node --test` | 제어 가능한 시간·입력으로 PRD 수치, 경계, 탄환 출발/간격, 충돌 중복, 승패 우선순위, 초기화를 검사 |
| Playwright Chromium | 실제 버튼·키로 시작/이동/발사/입력 정리/종료 안내를 확인하고 DOM 및 Canvas 픽셀·스크린샷으로 관찰 |
| 문서 대조 | UI·README 조작 안내와 PRD 일치 확인 |

브라우저 테스트에 모델 전역 노출, 치트 주입, 테스트 전용 승리 버튼을 넣지 않는다.
자연 플레이로 재현하기 어려운 경계는 모델 테스트에서 검증하며 실제 입력 검증과 혼동하지 않는다.
실제 사람 플레이·다른 OS/브라우저는 자동 Chromium 결과와 구분한다.

## 개발과 공개

현재 환경 Node v24.14.1 LTS, npm 10.8.3을 확인했다.
구현 단계에서 호환되는 안정판 Vite·Playwright를 조사해 manifest에 정확한 버전을 정의한 뒤 설치하고 lockfile을 커밋한다.
실제 npm 명령은 구현 시 manifest에서 확정한다.
Vite dev/E2E는 5173, preview는 4173을 사용하고 엄격한 포트 지정으로 충돌을 숨기지 않는다.
서버 시작 세션이 PID·포트·URL과 종료를 관리하며 모르는 프로세스는 종료하지 않는다.

Vite base는 `./`로 설정한다. 외부 런타임 이미지·CDN 없이 로컬 자산과 도형만 사용한다.
GitHub Actions에서 Node 검사·브라우저 검사·빌드 후 `dist`만 Pages로 배포한다.
PR에서는 검사만 수행하고 main만 배포하며 상세 권한·공식 action SHA는 배포 단계에서 확정한다.
