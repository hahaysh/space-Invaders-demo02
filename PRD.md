# 우주 방어 제품 요구사항

`ideation.md`의 첫 배포 경험을 제품 기준으로 정의한다. 첫 버전은 일시정지·난이도·목숨 없이 구현한다.

## 공통 수치

좌표 원점은 Canvas 왼쪽 위, 사각형 위치도 왼쪽 위다. 시간 단위는 초, 속도는 px/s다.

| 항목 | 기본값 |
|---|---|
| 화면·플레이어 | 800×600, 크기 40×20, 초기 위치 (380,550), 속도 320 |
| 플레이어 탄환 | 4×12, 속도 600, 발사 간격 0.2초, 화면 밖 제거 |
| 탄환 출발 | 플레이어 수평 중앙, 탄환 바닥 = 플레이어 위쪽; 초기 (398,538) |
| 적 편대 | 3행×8열=24, 크기 40×24, 위치 (112+72×열,72+48×행) |
| 적 이동 | 초기 오른쪽, 속도 64, 가장자리에서 방향 반전과 24 하강 |
| 점수·방어선 | 적 하나 10점, 최대 240점, 적 바닥이 y=520에 닿으면 패배 |

## 요구사항과 수용 기준

| ID | 요구사항·관찰 가능한 완료 조건 | 검증 |
|---|---|---|
| R01 | title에서 제목, 조작 안내, 시작 버튼을 표시한다. 시작 버튼 또는 반복 아닌 Enter로 playing에 진입한다. | DOM·실제 키 입력 |
| R02 | playing에서 좌우 화살표 또는 A/D로 이동한다. 화면 밖으로 나가지 않고 양방향 동시 입력이면 정지한다. | 모델 경계·Canvas 이동 |
| R03 | playing에서 Space를 누르고 있으면 기준 간격으로 위로 발사한다. 출발 위치·속도·화면 밖 제거는 기준표를 따른다. | 모델 시간·실제 입력·Canvas |
| R04 | 편대가 기준 배치에서 오른쪽으로 이동하며 가장자리에서 방향을 반전하고 하강한다. | 모델 경계·Canvas |
| R05 | 탄환 하나는 적 하나만 제거한다. 제거된 적은 중복 득점하지 않는다. 점수는 화면에 표시한다. | 모델 충돌·DOM 점수 |
| R06 | 충돌 처리 뒤 남은 적이 없으면 won, 그렇지 않고 적 바닥이 방어선에 닿으면 lost다. 같은 갱신의 마지막 적 제거는 패배보다 승리가 우선이다. | 모델 우선순위·결과 화면 |
| R07 | won/lost에는 결과와 재시작 버튼을 표시한다. 종료 후 이동·발사·시간 진행은 멈춘다. | 모델 상태·Canvas 정지 |
| R08 | won/lost에서 재시작 버튼 또는 반복 아닌 R로 playing의 새 게임을 시작한다. 점수·적·플레이어·탄환·시간·입력을 모두 초기화한다. playing에서는 R/Enter로 초기화하지 않는다. | 모델 초기화·DOM·실제 키 |
| R09 | 단발 동작은 키 반복을 무시한다. 필요한 게임 키만 기본 동작을 막고 Space 스크롤을 방지한다. blur/페이지 비표시 때 입력을 비우며 첫 버전에서는 자동 일시정지하지 않는다. | 실제 브라우저 이벤트 |
| R10 | UI와 README의 제목·키·규칙이 구현과 일치한다. 자동 모델 검사와 브라우저 화면 검증을 수행하고 미실행은 구분한다. | 문서 대조·검증 기록 |

## 상태와 범위

title → playing → won/lost → playing 흐름을 따른다.
title과 종료 화면에서는 진행 중 전용 이동·발사를 적용하지 않는다.
버튼은 키보드로 접근 가능하고 상태·점수·조작 설명은 DOM으로 읽을 수 있게 한다.
도형 기반의 데스크톱 브라우저 게임이며 적 탄환·적 공격, 외부 이미지/CDN, 로그인, 사운드, 모바일 조작은 제외한다.
일시정지·난이도·목숨은 첫 공개 이후 별도 개선 이슈에서 요구사항을 갱신한다.
