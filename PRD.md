# 우주 방어 제품 요구사항

`ideation.md`의 첫 배포 경험을 제품 기준으로 정의한다. [이슈 #7](https://github.com/hahaysh/space-Invaders-demo02/issues/7)의 일시정지와 [이슈 #9의 난이도 공개](https://github.com/hahaysh/space-Invaders-demo02/issues/9#issuecomment-5660296207)는 완료되었다. [이슈 #11](https://github.com/hahaysh/space-Invaders-demo02/issues/11)의 목숨·재도전은 09-01 사용자 위임 승인 설계이며 아직 미구현이다. 아래 방어선 도달 즉시 lost 조건을 목숨 판정으로 명시적으로 대체하고, 그 외 기존 수치·게임/P·난이도 계약은 보존한다.

## 공통 수치

좌표 원점은 Canvas 왼쪽 위, 사각형 위치도 왼쪽 위다. 시간 단위는 초, 속도는 px/s다.

| 항목 | 기본값 |
|---|---|
| 화면·플레이어 | 800×600, 크기 40×20, 초기 위치 (380,550), 속도 320 |
| 플레이어 탄환 | 4×12, 속도 600, 발사 간격 0.2초, 화면 밖 제거 |
| 탄환 출발 | 플레이어 수평 중앙, 탄환 바닥 = 플레이어 위쪽; 초기 (398,538) |
| 적 편대 | 3행×8열=24, 크기 40×24, 위치 (112+72×열,72+48×행) |
| 적 이동 | 초기 오른쪽, 속도 쉬움 32 / 보통 64 / 어려움 96, 가장자리에서 방향 반전과 24 하강 |
| 점수·방어선 | 적 하나 10점, 시도당 최대 240점, 적 바닥이 y=520에 닿으면 R17 판정 |
| 새 게임 목숨 | 3 (title 시작 및 won/lost 뒤 새 게임) |

## 요구사항과 수용 기준

| ID | 요구사항·관찰 가능한 완료 조건 | 검증 |
|---|---|---|
| R01 | title에서 제목, 조작 안내, 시작 버튼을 표시한다. 시작 버튼 또는 반복 아닌 Enter로 playing에 진입한다. select 포커스 예외는 R16을 따른다. | DOM·실제 키 입력 |
| R02 | playing에서 좌우 화살표 또는 A/D로 이동한다. 화면 밖으로 나가지 않고 양방향 동시 입력이면 정지한다. | 모델 경계·Canvas 이동 |
| R03 | playing에서 Space를 누르고 있으면 기준 간격으로 위로 발사한다. 출발 위치·속도·화면 밖 제거는 기준표를 따른다. | 모델 시간·실제 입력·Canvas |
| R04 | 편대가 기준 배치에서 오른쪽으로 이동하며 가장자리에서 방향을 반전하고 하강한다. | 모델 경계·Canvas |
| R05 | 탄환 하나는 적 하나만 제거한다. 제거된 적은 중복 득점하지 않는다. 점수는 화면에 표시한다. | 모델 충돌·DOM 점수 |
| R06 | 충돌 처리 뒤 남은 적이 없으면 won이며 남은 목숨을 보존한다. 그렇지 않고 생존 적 바닥이 방어선에 닿으면 R17을 적용한다. 같은 갱신의 마지막 적 제거는 목숨 차감보다 승리가 우선이다. | 모델 우선순위·결과 화면 |
| R07 | won/lost에는 결과와 재시작 버튼을 표시한다. 종료 후 이동·발사·시간 진행은 멈춘다. | 모델 상태·Canvas 정지 |
| R08 | won/lost에서 재시작 버튼 또는 반복 아닌 R로 playing의 새 게임을 시작한다. 목숨은 공통 수치표로, 점수·적·플레이어·탄환·편대 방향·cooldown·시간·입력은 초기 상태로 되돌리고 선택 난이도를 적용한다. playing/paused에서는 R/Enter로 초기화하지 않는다. retry는 새 게임이 아니라 R19/R20을 따른다. | 모델 초기화·DOM·실제 키 |
| R09 | 단발 동작은 키 반복을 무시한다. 필요한 게임 키만 기본 동작을 막고 Space 스크롤을 방지한다. paused에서도 이동·발사 키의 기본 동작은 막되 진행 입력을 저장하지 않는다. select의 native 입력은 R16에 따라 제외한다. blur/페이지 비표시 때 입력만 비우며 자동 일시정지·재개하지 않는다. | 실제 브라우저 이벤트 |
| R10 | UI와 README의 제목·키·규칙이 구현과 일치한다. 자동 모델 검사와 브라우저 화면 검증을 수행하고 미실행은 구분한다. | 문서 대조·검증 기록 |
| R11 | playing/paused에서만 반복 아닌 P로 서로 전환한다. title/retry/won/lost에서 P는 무시하며 기존 시작·종료·재시작 동작을 보존한다. 일시정지 상태와 P 재개 안내는 DOM으로 읽을 수 있다. | 모델 상태 전환·실제 P 입력·DOM |
| R12 | paused에서는 플레이어·적·탄환·편대 방향·점수·게임 시간·발사 cooldown이 모두 불변이다. 정지·재개는 상태만 바꾸고 같은 한 판을 보존한다. 전환마다 입력을 비우며 정지 중 입력을 예약하지 않는다. 재개 시 시계 기준을 재설정해 정지 시간만큼 이동·연속 발사가 몰리지 않고 기존 속도와 남은 cooldown으로 이어진다. 반복 전환에도 루프는 하나다. | 모델 전체 상태·시간 경계·실제 입력·Canvas·속도 회귀 |
| R13 | 페이지 새로고침 시 난이도는 보통이며 저장하지 않는다. 선택 중인 pending과 현재 판의 current를 분리한다. title/won/lost에서만 pending을 바꿀 수 있고 playing/paused/retry에서는 UI와 모델 모두 변경을 막는다. | 모델 초기값·상태별 잠금·브라우저 새로고침/DOM |
| R14 | 새 게임 시작/재시작 때만 pending을 current로 적용해 새 판을 만들고 적의 수평 속도만 공통 수치표에 따라 바꾼다. 종료 화면에서 다음 난이도를 골라도 종료한 판의 current는 그대로다. 같은 페이지의 다음 게임은 선택을 유지하며 P 정지·재개는 두 설정을 보존한다. retry의 다음 시도는 pending을 새로 적용하지 않고 R19를 따른다. 난이도 자체는 적 수·점수·하강량·승패 규칙을 바꾸지 않는다. | 모델 적용 시점·실제 속도·기존 게임/P 회귀 |
| R15 | 허용하지 않은 난이도 값은 명시적 오류이며 보통으로 조용히 대체하지 않는다. 실패 시 선택·현재 판·진행 데이터는 보존하고 UI에서 발생한 오류는 읽을 수 있게 알린다. | 모델 오류·무변경·DOM 오류 안내 |
| R16 | 레이블이 있는 기본 HTML select를 키보드로 조작한다. select에 포커스가 있을 때 화살표/Space/Enter는 선택 조작이며 게임 핸들러가 가로채거나 시작·이동·발사하지 않는다. 선택 후 시작/재시작 버튼으로 이동해 실행할 수 있고 select 밖의 기존 게임 단축키는 유지한다. | 실제 Tab·select 키 입력·DOM·기본 동작 방지 여부 |
| R17 | playing에서 충돌 후 생존 적이 방어선에 도달하면 같은 갱신에 여러 적이 닿아도 목숨을 정확히 한 번 차감한다. 남은 목숨이 양수면 retry, 0이면 lost로 즉시 전환하고 나머지 갱신을 중단한다. 이후 프레임에는 중복 차감하지 않는다. | 모델 동시 도달·하위 갱신·반복 갱신·DOM 목숨 |
| R18 | retry/lost에서는 실패 직후 시도의 화면·점수·게임 시간을 동결한다. 자동으로 새 시도를 시작하거나 점수를 초기화하지 않는다. retry에 남은 목숨·실패 안내·다음 시도 조작을 DOM으로 표시하고, won/lost에도 남은 목숨과 결과를 읽을 수 있게 표시한다. | 전체 모델 불변·DOM·Canvas 동결 |
| R19 | retry에서 반복 아닌 Enter 또는 다음 시도 버튼으로 playing의 새 시도를 시작한다. 남은 목숨·current 난이도만 보존하고 pending은 그 current로 맞춘다. 점수는 0, 적·플레이어·탄환·편대 방향·cooldown·게임 시간·프레임 시계 기준·입력은 초기화한다. 실패 시도 점수를 누적하지 않고 대기 시간만큼 이동·발사를 몰아서 처리하지 않는다. | 모델 보존/초기화·실제 키/버튼·첫 프레임 |
| R20 | retry에서는 R/P·이동·게임 발사·repeat Enter를 무시하고 진행 입력을 저장하지 않는다. 난이도는 R13에 따라 잠근다. 실패 전이나 대기 중 누른 키와 그 repeat가 다음 시도에 남지 않으며 이동·발사는 키를 떼고 새로 눌러야 한다. 다음 시도 요청은 retry 밖에서 무변경이다. | 모델 상태 경계·실제 입력·잠금·반복 전환 |

## 상태와 범위

title → playing → won/lost → 새 게임 흐름과 playing ↔ paused를 유지하며, playing → retry → 다음 시도 playing을 추가한다.
title·paused·retry·종료 화면에서는 진행 중 전용 이동·발사를 적용하지 않는다.
버튼은 키보드로 접근 가능하고 상태·점수·조작 설명은 DOM으로 읽을 수 있게 한다.
도형 기반의 데스크톱 브라우저 게임이며 적 탄환·적 공격, 외부 이미지/CDN, 로그인, 사운드, 모바일 조작은 제외한다.
09-01은 목숨·재도전 문서 설계만 수행한다. 09-02 별도 지시 뒤 구현·회귀·빌드 및 병합 후 실제 공개를 확인한다.
자동 레벨 상승·자동 blur 일시정지·위에 정의하지 않은 단축키는 제외하며 기존 P 일시정지·재개는 유지한다.
